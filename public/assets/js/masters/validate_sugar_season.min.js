jQuery.validator.addMethod(
    "sugarSeasonFormat",
    function (value, element) {
        return this.optional(element) || /^\d{4}-\d{4}$/.test(value);
    },
    "Sugar season must be in the format YYYY-YYYY."
);

jQuery(function () {
    $(".js-validation-sugarseason").validate({
        rules: {
            sugar_season: {
                required: true,
                minlength: 9,
                maxlength: 9,
                sugarSeasonFormat: true,
            },
            status: {
                required: true,
            },
        },
        messages: {
            sugar_season: {
                required: "Please enter a sugar season.",
                minlength:
                    "Sugar season must be exactly 9 characters (YYYY-YYYY).",
                maxlength:
                    "Sugar season must be exactly 9 characters (YYYY-YYYY).",
                sugarSeasonFormat:
                    "Sugar season must be in the format YYYY-YYYY.",
            },
            status: {
                required: "Please select a status.",
            },
        },
        errorClass: "is-invalid",
        validClass: "is-valid",
        errorElement: "div",
        highlight: function (element) {
            $(element).addClass("is-invalid").removeClass("is-valid");
        },
        unhighlight: function (element) {
            $(element).addClass("is-valid").removeClass("is-invalid");
        },
        errorPlacement: function (error, element) {
            error.addClass("invalid-feedback");
            if (element.parent(".input-group").length) {
                element.parent().after(error);
            } else if (element.closest(".form-group").length) {
                element.closest(".form-group").append(error);
            } else if (element.closest(".mb-3").length) {
                element.closest(".mb-3").append(error);
            } else {
                element.after(error);
            }
        },
        submitHandler: function (form) {
            form.submit();
        },
        invalidHandler: function (_, validator) {
            if (validator.errorList.length) {
                $(validator.errorList[0].element).trigger("focus");
            }
        },
    });

    $(".js-validation-sugarseason").on("submit", function (e) {
        if (!$(this).valid()) {
            e.preventDefault();
        }
    });
});
