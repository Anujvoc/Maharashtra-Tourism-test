$(document).ready(function () {

    // Ensure Dashmix is loaded before using its helpers
    if (typeof Dashmix !== "undefined" && Dashmix.helpersOnLoad) {
        Dashmix.helpersOnLoad(["jq-validation"]);
    } else {
        console.error(
            "Dashmix is not loaded. Please include Dashmix before this script."
        );
        return;
    }
    // Initialize form validation
    $(".js-validation-branch").validate({
        rules: {
            state_id: {
                required: true,
                // monthName: true, // Custom rule
            },
	    district_id: {
                required: true,
                // monthName: true, // Custom rule
            },
	    nodel_officer_id{
                required: true,
                // monthName: true, // Custom rule
            },
	    branch_name: {
                required: true,
                // monthName: true, // Custom rule
            },
            status: {
                required: true,
            },
        },
        messages: {
            state_id{
                required: "Please Select State name.",
                // monthName: "Please enter a valid full month name.",
            },
 	    district_id{
                required: "Please Select District name.",
                // monthName: "Please enter a valid full month name.",
            },
            nodel_officer_id{
                required: "Please enter a Nodel officer name.",
                // monthName: "Please enter a valid full month name.",
            },
	    branch_name{
                required: "Please enter a Branch name.",
                // monthName: "Please enter a valid full month name.",
            },
            status: {
                required: "Please select a status.",
            },
        },
        errorClass: "is-invalid",
        validClass: "is-valid",
        errorElement: "div",
        highlight: function (element) {
            $(element).addClass("is-invalid").removeClass("is-valid");
        },
        unhighlight: function (element) {
            $(element).addClass("is-valid").removeClass("is-invalid");
        },
        errorPlacement: function (error, element) {
            error.addClass("invalid-feedback");
            if (element.closest(".form-group").length) {
                element.closest(".form-group").append(error);
            } else if (element.closest(".mb-3").length) {
                element.closest(".mb-3").append(error);
            } else {
                element.after(error);
            }
        },
        submitHandler: function (form) {
            form.submit();
        },
        invalidHandler: function (event, validator) {
            if (validator.errorList.length) {
                $(validator.errorList[0].element).focus();
            }
        },
    });

    // Prevent manual submission if invalid
    $(".js-validation-branch").on("submit", function (e) {
        if (!$(this).valid()) {
            e.preventDefault();
        }
    });
});
