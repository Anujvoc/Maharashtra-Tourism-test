$(document).ready(function () {

    // Ensure Dashmix is loaded before using its helpers

    if (typeof Dashmix !== "undefined" && Dashmix.helpersOnLoad) {

        Dashmix.helpersOnLoad(["jq-validation"]);

    } else {

        console.error(

            "Dashmix is not loaded. Please include Dashmix before this script."

        );

        return;

    }



    // Add custom validator for 
    //  name: only letters and spaces, max 25 chars, no numbers

    $.validator.addMethod(

        "teacher_name",

        function (value, element) {

            return this.optional(element) || /^[A-Za-z\s]{1,25}$/.test(value);

        },

        "Teacher name must be letters only (max 25 characters, no numbers)."

    );



    // Initialize form validation

    $(".js-validation-teacher").validate({

        rules: {

            teacher_name: {

                required: true,

                teacher_name: true,

                maxlength: 30,

            },

            // country: {

            //     required: true,

            // },

            status: {

                required: true,

            },

        },

        messages: {

            teacher_name: {

                required: "Please enter a Teacher name.",

                // stateName:

                //     "State name must be letters only (max 30 characters, no numbers).",

                maxlength: "Teacher name cannot exceed 25 characters.",

            },

            // country: {

            //     required: "Please select a country.",

            // },

            status: {

                required: "Please select a status.",

            },

        },

        errorClass: "is-invalid",

        validClass: "is-valid",

        errorElement: "div",

        highlight: function (element) {

            $(element).addClass("is-invalid").removeClass("is-valid");

        },

        unhighlight: function (element) {

            $(element).addClass("is-valid").removeClass("is-invalid");

        },

        errorPlacement: function (error, element) {

            error.addClass("invalid-feedback");

            if (element.closest(".form-group").length) {

                element.closest(".form-group").append(error);

            } else if (element.closest(".mb-3").length) {

                element.closest(".mb-3").append(error);

            } else {

                element.after(error);

            }

        },

        submitHandler: function (form) {

            form.submit();

        },

        invalidHandler: function (event, validator) {

            if (validator.errorList.length) {

                $(validator.errorList[0].element).focus();

            }

        },

    });



    // Prevent manual submission if invalid

    $(".js-validation-teacher").on("submit", function (e) {

        if (!$(this).valid()) {

            e.preventDefault();

        }

    });

});

